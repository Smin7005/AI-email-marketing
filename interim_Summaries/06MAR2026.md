# 1. From the previous git commit (2e5ad404ef305632493e89a0bc7ceed049562d7e), we have implemented a task list of onboarding user guide, but now it disappered after I click on the 'restart user guide' button. Also, when I click on that button, only the sidebar tour will refresh, but the user guide on other tabs didn't work.

## Plan:

**Bug 1 — Checklist never appeared**

`OnboardingChecklist` was defined but never placed in the component tree. Added it to `layout.tsx` inside `<NextStepWrapper>` so it renders on every dashboard page.

**Bug 2 — Checklist disappeared on restart / didn't update when tours completed**

The original `useEffect` in `OnboardingChecklist.tsx` only re-ran when `user` changed. Two things were broken: clicking restart cleared localStorage but the checklist's React state stayed stale, and completing a tour on another page set a localStorage key that the checklist had no way to detect.

The fix moves the evaluation logic into an inner `evaluate` function that runs on mount AND listens to two custom window events:

- `onboarding-reset` — dispatched from `Sidebar.tsx` when restart is clicked, causing the checklist to reappear with all steps unchecked.
- `tour-done` — dispatched from `nextstep-provider.tsx` when any tour completes or is skipped, causing the checklist to mark that step complete in real time.

**Files Changed**

- `src/app/(dashboard)/layout.tsx` — import and render `<OnboardingChecklist />` inside `<NextStepWrapper>`
- `src/components/onboarding/OnboardingChecklist.tsx` — refactor `useEffect` to register `onboarding-reset` and `tour-done` event listeners so the checklist re-evaluates without needing a page reload
- `src/components/providers/nextstep-provider.tsx` — dispatch `tour-done` custom event after `localStorage.setItem` in both `handleTourComplete` and `handleTourSkip`
- `src/components/dashboard/Sidebar.tsx` — dispatch `onboarding-reset` event after clearing localStorage in `handleRestartTour`

## Follow-up 1:

All four changes are done. Here's a summary of what was fixed and why:

Bug 1 — Checklist never appeared:
- OnboardingChecklist was defined but never placed in the component tree. Added it to layout.tsx inside <NextStepWrapper> so it renders on every dashboard page.   

Bug 2 — Checklist disappeared on restart / didn't update when tours completed:
- The original useEffect only re-ran when user changed. 

Two things were broken:

  - Clicking restart cleared localStorage but the checklist's React state stayed stale (it still showed old completed steps or stayed hidden).
  - Completing a tour on another page (e.g. senders tour) set a localStorage key, but the checklist had no way to know about it.

The fix moves the evaluation logic into an inner evaluate function that runs on mount AND listens to two custom events:

  - onboarding-reset — dispatched from Sidebar.tsx when restart is clicked, causing the checklist to reappear with all steps unchecked.
  - tour-done — dispatched from nextstep-provider.tsx when any tour completes or is skipped, causing the checklist to mark that step complete in real time.

## Follow-up 2:

All 6 files updated. Here's the root cause and what changed:

Root cause: Every tour/checklist check had an isNewUser time-gate (7–14 days). Clicking restart only called startNextStep('sidebar-tour') directly — which bypasses the gate — but the checklist and all other page tours still checked isNewUser and silently skipped for accounts older than those windows.

Fix — onboarding-force-restart localStorage flag:

  - Sidebar.tsx — sets onboarding-force-restart = 'true' alongside clearing the other keys on restart
  - OnboardingChecklist.tsx — handleReset (fired on onboarding-reset event) now directly sets isVisible = true and clears completedSteps, bypassing the time check entirely. The regular evaluate also checks forceRestart for subsequent re-evaluations.
  - TourAutoStart.tsx — condition changed from isNewUser && !hasSeenTour to (isNewUser || forceRestart) && !hasSeenTour
  - senders/page.tsx, campaigns/create/page.tsx, campaigns/[id]/page.tsx — same condition fix in their inline useEffects

The forceRestart flag persists in localStorage until the next restart. Once a tour on a page is seen (tour-*-done set), that page won't re-trigger it — hasSeenTour guards against that.

# 2. On the Leads tab, that 'Browse Results' onboarding component should move up, also the display effect of 'Getting Started' task list is weried.

## Plan:

**Issue 1 — Browse Results tour card overlaps pagination at the bottom**

Root cause: The tour step targets `#leads-grid` (the full ag-Grid element with `domLayout="autoHeight"` spanning most of the viewport). nextstepjs with `side: 'top'` cannot find space above the tall element and falls back to bottom positioning, colliding with the pagination bar.

Fix: Add `id="leads-results-summary"` to the results summary div in `leads/page.tsx` (just above the table). Update the tour step to target `#leads-results-summary` with `side: 'bottom'`, so the tooltip appears just below the "Showing X of Y results" line — directly above the table and visually centred in the viewport.

**Issue 2 — Getting Started checklist overflows the viewport**

Root cause: The checklist card is `fixed bottom-6 right-6` with no max-height. With all 4 steps incomplete (each showing title + description), the card is tall enough to clip outside the viewport.

Fix: Add `max-h-[calc(100vh-6rem)] overflow-y-auto` to the outer div of `OnboardingChecklist.tsx` so the card scrolls internally rather than overflowing.

**Files Changed**

- `src/app/(dashboard)/leads/page.tsx` — add `id="leads-results-summary"` to the results summary div
- `src/lib/onboarding/tours.ts` — leads-tour last step: selector → `#leads-results-summary`, side → `'bottom'`
- `src/components/onboarding/OnboardingChecklist.tsx` — outer div: add `max-h-[calc(100vh-6rem)] overflow-y-auto`

## Follow-up 1:

All done. Three code changes made:

  1. leads/page.tsx — added id="leads-results-summary" to the results count div above the table
  2. tours.ts — Browse Results step now targets #leads-results-summary with side: 'bottom', positioning the tooltip just below the results count line (well above the pagination bar)
  3. OnboardingChecklist.tsx — outer div now has max-h-[calc(100vh-6rem)] overflow-y-auto so the checklist card scrolls internally instead of clipping outside the viewport

## Follow-up 2: the last step of user guide on Leads tab should point to 1st contact of the table, not the header.

Changed the selector from #leads-results-summary to .ag-row[row-index="0"] — this targets the first data row in the ag-Grid table. The tooltip will appear below that row with side: 'bottom', pointing directly at the first contact in the results.

# 3. On the Collections tab, the last step of the onboarding tour ('Your Collections') is appearing at the bottom of the viewport, outside the visible page frame. It should display near the top of the collections grid instead.

## Plan & Fix:

Same root cause as the Leads page Browse Results step: the "Your Collections" tour step targeted `#collections-grid` (the full grid spanning most of the viewport) with `side: 'top'`, causing nextstepjs to fall back to bottom positioning outside the page frame.

The `#collections-header` id already exists on the header div at the top of the collections page. Changed the tour step to target `#collections-header` with `side: 'bottom'`, so the tooltip appears just below the page header — well within the visible frame.

**File Changed**

- `src/lib/onboarding/tours.ts` — collections-tour last step: selector → `#collections-header`, side → `'bottom'`

## Follow-up 1: 

Done. The '#collections-header' id was already on the page header div, so only one file needed changing: the last step of collections-tour in tours.ts now targets '#collections-header' with side: 'bottom'. The tooltip will appear just below the "My Collections" heading, well within the visible page frame.

# 4. Now we need to update the 'Getting started' list. We just need 3 tasks, 1st task should be 'Website Overview' which is the Sidebar tour guide, when user finshed the Sidebar tour guide, this task should be marked as finished on 'Getting started' list. Also I want to integrate the 'Find your 1st Leads' and 'Create a Collection' into 'Launch your 1st Campaign', so task 2 would be complicated than others. As for task 3 that is Add your sender, after user click on 'Add sender' button, we need also have 2 steps of user guides, 1 for personal email, 1 for custom domain.

Plan:

Redesign the Getting Started checklist from 4 tasks down to 3, restructuring around the natural user journey.

**Task 1 — Website Overview** (storageKey: `tour-sidebar-tour-done`): Marked complete when the sidebar tour finishes. No changes to the sidebar tour itself — `nextstep-provider.tsx` already sets this key on complete/skip.

**Task 2 — Launch Your First Campaign** (storageKey: `tour-create-campaign-tour-done`): Merges Find Leads + Create Collection + Launch Campaign into one checklist item. The underlying page tours for leads, collections, and campaign creation still auto-start when those pages are visited; they just no longer have separate checklist items. Marked complete when the create-campaign tour finishes.

**Task 3 — Add Your Sender** (storageKey: `tour-add-sender-dialog-tour-done`): A new 2-step dialog tour (`add-sender-dialog-tour`) auto-starts when the user opens the Add Sender dialog. Step 1 highlights the Personal Email tab, step 2 highlights the Custom Domain tab. The tour is triggered by a `useEffect` in `AddSenderDialog.tsx` watching the `open` prop, following the same `isNewUser || forceRestart` pattern as other page tours.

**Files Changed**

- `src/components/onboarding/OnboardingChecklist.tsx` — replaced STEPS array with 3 new steps (overview, campaign, sender) using the storageKeys above
- `src/lib/onboarding/tours.ts` — added `add-sender-dialog-tour` with 2 steps targeting `#add-sender-tab-personal` and `#add-sender-tab-domain`
- `src/components/senders/AddSenderDialog.tsx` — added `id` attributes to both TabsTrigger elements; added `useNextStep` + `useUser` imports and a `useEffect` to auto-start the dialog tour when the dialog opens

## Follow-up 1: Now after the 'Website Overview', we need more steps for 'Launch your 1st campaign', for example, guide user to click on 'Leads tab', then start the leads tab user guide, then guide user to click on 'Collections tab', then start collections tab user guide, followed by that, guide user to click on 'Campaigns tab' then start campaigns tab user guide. Also on 'Campaigns tab', add step to guide user click on 'Create New Campaign' button. 

All 9 files updated. Here's a summary of what was built: 

  campaign-launch-guide tour (11 steps) in tours.ts:
  - Step 1: Points to #sidebar-nav-leads → nextRoute: '/leads'
  - Steps 2–5: Full leads page guide (filters, search, industry, results)
  - Step 6: Points to #sidebar-nav-collections → nextRoute: '/collections'
  - Steps 7–8: Collections guide (header, create button)
  - Step 9: Points to #sidebar-nav-campaigns → nextRoute: '/campaigns'
  - Steps 10–11: Campaigns guide (header, Create New Campaign button)

  Tour suppression — All individual page auto-start tours (TourAutoStart, senders, campaigns/create, campaigns/[id], AddSenderDialog) now check onboarding-campaign-guide-active and skip if it's set.
  
  Lifecycle — campaigns/page.tsx sets the active flag and starts the guide. nextstep-provider.tsx clears it on complete/skip, and on complete also marks leads-tour and collections-tour as done (since that content was covered). Sidebar restart also clears the flag. 

  Checklist — "Launch Your First Campaign" now links to /campaigns and tracks tour-campaign-launch-guide-done.

## Follow-up 2: acutally I mean, after 'Website Overview' is finished, just simply add 1 step to guide user click on Leads tab, after user finish Leads tab user guide, we add another 1 step to guide user click on Collections tab, and so on. But what you did is add many steps, when user click on Campaigns tab, that's not what we planned.

Plan (Revised):

Replace the big `campaign-launch-guide` tour with small single-step bridge tours chained together by a new `TourChainer` component.

Desired flow: sidebar-tour ends → 1-step bridge "Go to Leads" → leads-tour auto-starts → 1-step bridge "Go to Collections" → collections-tour auto-starts → 1-step bridge "Go to Campaigns" → campaigns-list-tour auto-starts (2 steps: page header + Create New Campaign button) → checklist marks complete.

Four new small tours added to tours.ts: `go-to-leads`, `go-to-collections`, `go-to-campaigns` (each 1 step with `nextRoute`), plus `campaigns-list-tour` (2 steps on campaigns list page). A new `TourChainer.tsx` component listens for `tour-done` events and starts the next bridge tour in the chain. The big 11-step `campaign-launch-guide` is removed. All `campaignGuideActive` flag checks are cleaned up from page files since the suppression pattern is no longer needed.

**Files Changed**

- `src/lib/onboarding/tours.ts` — remove `campaign-launch-guide`; add `go-to-leads`, `go-to-collections`, `go-to-campaigns`, `campaigns-list-tour`
- `src/components/onboarding/TourChainer.tsx` — new component (created)
- `src/app/(dashboard)/layout.tsx` — add `<TourChainer />`
- `src/app/(dashboard)/campaigns/page.tsx` — replace trigger with `campaigns-list-tour`; remove active flag
- `src/components/onboarding/OnboardingChecklist.tsx` — storageKey → `tour-campaigns-list-tour-done`, href → `/campaigns`
- `src/components/providers/nextstep-provider.tsx` — remove `campaign-launch-guide` specific logic
- `src/components/dashboard/Sidebar.tsx` — remove `onboarding-campaign-guide-active` cleanup
- `src/components/onboarding/TourAutoStart.tsx` — remove `campaignGuideActive` check
- `src/app/(dashboard)/senders/page.tsx` — remove `campaignGuideActive` check
- `src/app/(dashboard)/campaigns/create/page.tsx` — remove `campaignGuideActive` check
- `src/app/(dashboard)/campaigns/[id]/page.tsx` — remove `campaignGuideActive` check
- `src/components/senders/AddSenderDialog.tsx` — remove `campaignGuideActive` check

## Follow-up 3: 
All 11 changes are complete. Here's a summary of what was implemented:

  New file:
  - src/components/onboarding/TourChainer.tsx — listens for tour-done events and chains: sidebar-tour → go-to-leads, leads-tour → go-to-collections, collections-tour → go-to-campaigns
  
  Updated files:
  - layout.tsx — added <TourChainer /> inside NextStepWrapper
  - campaigns/page.tsx — replaced campaign-launch-guide trigger (with campaignGuideActive flag) with campaigns-list-tour trigger (no flag)        
  - OnboardingChecklist.tsx — campaign step storageKey changed to tour-campaigns-list-tour-done
  - nextstep-provider.tsx — removed all campaign-launch-guide special-casing
  - Sidebar.tsx — removed localStorage.removeItem('onboarding-campaign-guide-active') from restart handler
  - TourAutoStart.tsx — removed campaignGuideActive suppression check
  - senders/page.tsx, campaigns/create/page.tsx, campaigns/[id]/page.tsx, AddSenderDialog.tsx — all removed their campaignGuideActive suppression checks

  Flow: sidebar-tour → (TourChainer) → go-to-leads bridge → /leads auto-starts leads-tour → (TourChainer) → go-to-collections bridge → /collections auto-starts collections-tour → (TourChainer) → go-to-campaigns bridge → /campaigns auto-starts campaigns-list-tour → checklist marks complete.

